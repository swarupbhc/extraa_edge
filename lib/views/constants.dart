import 'package:flutter/material.dart';

const EdgeInsets primaryPagePadding =
    EdgeInsets.symmetric(horizontal: 20.0, vertical: 15.0);

const SizedBox primaryColumnSpacing = SizedBox(height: 15.0);
const SizedBox primaryMaxColSpacing = SizedBox(height: 50.0);

const SizedBox primaryRowSpacing = SizedBox(width: 15.0);

const primaryHeadingStyle =
    TextStyle(fontSize: 25.0, fontWeight: FontWeight.bold);

const primaryTitleStyle =
    TextStyle(fontSize: 17.0, fontWeight: FontWeight.bold, color: Colors.black);

const primarySubtitleStyle = TextStyle(fontSize: 15.0);
